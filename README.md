# Composer’s Toolbox — Build 164

Complete 78-file Mac source checkpoint in [source/](source/).

## Retrieve and build

1. Click **Code → Download ZIP** on this repository.
2. Unzip it and open the **source** folder.
3. Double-click **Build-and-Sign.command** on your Mac.
4. The successful build reveals **Composers-Toolbox-Mac-Only-Build-164.pkg** for Transporter.

Your Mac needs the Apple signing identities in its Keychain and Xcode command-line tools. Private signing keys are not included.

## Status

The user reported build 164 **Ready for internal testing** after uploading it to Apple. This is a user-reported TestFlight processing status, not App Store review approval. The uploaded signed package and Apple processing logs are not included here.

All 78 source files match the complete build-164 download supplied in this session, byte for byte. The 60 resource checks passed. Native verification passed on the user's Mac for the preceding repair; the user subsequently completed the build-164 upload. Historical source notes are preserved unchanged within source/.

See [CHECKPOINT.md](CHECKPOINT.md) for provenance and verification. This repository contains the full source, sound assets, build script, checks, entitlements, and provisioning profile—not just this README.
